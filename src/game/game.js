import Board from './board';
import GameContext from './game-context';

class Game {
  constructor(el, options) {
    const {
      boardOptions,
      contextOptions,
      fieldOptions,
    } = options;
  }

  reset() {
  }
}

export default Game;
